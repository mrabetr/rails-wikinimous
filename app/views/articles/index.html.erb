<h1>Articles</h1>

<div class="list-group">
  <% @articles.each do |article| %>
    <%= link_to article.title, article_path(article), class: "list-group-item list-group-item-action" %>
  <% end %>
</div>
